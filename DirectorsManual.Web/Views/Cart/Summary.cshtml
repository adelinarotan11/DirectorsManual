@model DirectorsManual.Domain.Entities.Cart

    <ul class="nav navbar-nav navbar-collapse">
        <li>
            @Html.ActionLink("Basket", "Index", "Cart", new { returnUrl = Request.Url.PathAndQuery })
        </li>
    </ul>

    @*<ul class="nav navbar-nav navbar-right visible-xs">
        <li>
            <a href=@Url.Action("Index", "Cart", new { returnUrl = Request.Url.PathAndQuery })
               class="btn btn-default navbar-btn">
                <span class="glyphicon glyphicon-cloud"></span>
            </a>
        </li>
    </ul>*@


    @*<div class="navbar-text navbar-right">
            <b>Ваша корзина:</b>
            @Model.Lines.Sum(x => x.Quantity) товара(ов),
            @Model.ComputeTotalValue().ToString("# грн.")
        </div>*@

