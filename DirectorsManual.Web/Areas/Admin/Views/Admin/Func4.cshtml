@{
    ViewBag.Title = "Func4";
    Layout = "~/Areas/Admin/Views/Shared/_AdminLayout.cshtml";
}


<div>
    @using (Ajax.BeginForm("Func4Partial", new AjaxOptions { UpdateTargetId = "results" }))
    {<div class="panel">
            <div class="panel-heading">
                <h3>
                    Определить, сколько и каких деталей нужно для того,
                    чтобы собрать все изделия для заданного договора на поставку товаров
                    (сколько и каких изделий включает этот договор известно):
                </h3>
            </div>
            <div class="panel-body">
                <label>ID договора на поставку товаров:</label>
                <input type="number" name="ID" />
            </div>
            <div class="panel-footer">
                <input type="submit" value="Найти" class="btn btn-primary" />
            </div>
        </div>
    }
    <table id="results" class="table table-sm table-dark"></table>

</div>